# CLAUDE.md

This file is loaded automatically by Claude Code when working in this repository.

> **All project-level guidance lives in [`AGENTS.md`](AGENTS.md).** Read that first.

`AGENTS.md` is the single source of truth for:

- What the project is and the chosen stack
- Repo layout and where to find things
- Run / build / test commands
- Layered architecture rules (see also [`docs/architecture.md`](docs/architecture.md))
- Security & quality requirements (see also [`docs/security.md`](docs/security.md))
- Coding conventions (see also [`docs/conventions.md`](docs/conventions.md))
- The phased roadmap and the GitHub-issue / PR workflow

Other AI tools (Cursor, Copilot, etc.) should also read `AGENTS.md`. Keep this file as a thin pointer so every agent ends up at the same source of truth.
